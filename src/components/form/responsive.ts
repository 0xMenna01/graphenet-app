import { ResponsiveContainer } from '../../responsive/responsive'

export const avatarResponsive: ResponsiveContainer = {
   direction: ['column', 'column', 'row'],
}
export const avatarUpload: ResponsiveContainer = {
   alignSelf: ['center', 'center', 'start'],
}
export const connectBtn: ResponsiveContainer = {
   display: ['none', 'block', 'block'],
}
